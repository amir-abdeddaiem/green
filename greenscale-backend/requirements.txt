# Core API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0

# Database
sqlalchemy==2.0.23
psycopg[binary]==3.2.6

# Authentication & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.1.1

# File Uploads & Processing
python-multipart==0.0.6
reportlab==4.0.9

# OCR (Tesseract)
pytesseract==0.3.10
Pillow==10.3.0
# Optional but recommended to OCR PDFs (renders pages to images)
pypdfium2==4.30.0

# OCR (PaddleOCR) - OPTIONAL (higher accuracy for tables/AR)
# Install only if you want OCR_ENGINE=paddle and your platform/Python is supported:
#   pip install -r requirements-ocr-paddle.txt

# API Integration
httpx==0.25.2
requests==2.31.0

# AI/ML
google-generativeai>=0.0.0
openai>=1.0.0

# Environment & Configuration
python-dotenv==1.0.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3.post1

# Development (optional)
pytest==7.4.3
black==23.12.1
pylint==3.0.3
flake8==6.1.0
